# Social ScaleX
#
# Host is the Vercel deployment URL until a custom domain is registered.
# When one is bought, update the Sitemap: line below together with the
# references in index.html and public/sitemap.xml — see docs/MERGE_CHECKLIST.md §7.

User-agent: *
Allow: /

# The internal CRM. Gated by Supabase auth (signup is closed and the
# handle_new_user() trigger rejects any email not in allowed_emails), so this
# is not what keeps people out — it keeps the login page out of search results
# rather than having it surface on a query for the brand name.
Disallow: /crm
Disallow: /login

Sitemap: https://social-scalex.vercel.app/sitemap.xml
